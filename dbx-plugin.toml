schema_version = 1

[backend]
language = "rust"
directory = "backend"
binary = "dbx-plugin-clipboard"

[package]
include = ["assets", "ui"]

[dev]
ui_build = ["npm", "run", "build"]
ui_watch = ["npm", "run", "build:watch"]
